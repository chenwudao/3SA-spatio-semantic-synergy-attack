这 份文档将作 为 你整个   SAGA-MPC   研究 项 目的 “ 施工 蓝图 ” 。 我 为 你将前面的四 阶 段理 论计 划， 直接 转 化 为 了   AI   编 程助手（如   Cursor, GitHub Copilot   或   Gemini ）可以直接使用的 项 目 级  Prompt 。  你可以按照 阶 段 顺 序，将 对应 的模 块 化   Prompt   复制 给 你的   AI   编 程工具， 让 它 为 你生成底 层 的  PyTorch   代 码 架构、数据 处 理脚本和可 视 化代 码。  🔬   SAGA-MPC   项 目开 发蓝图 与代 码 生成指 令 库  本 项 目旨在构建一个高 维 度的多模 态对 抗攻 击 框架，融合 阶 段性空 间 注意力引 导 （ SAGA ）与多范 式 语义协 同（ MPC ）。 项 目主要使用   PyTorch   进 行深度学 习计 算，并依 赖   pandas   和   matplotlib   进  行后期的数据清洗与 验证 可 视 化。  阶 段一：基座构建与多范式注意力提取   (Sanity Check & IoU)  目 标 ：   构建基 础 的数据加 载 管道，并 编 写代 码 以提取不同架构模型（ CLIP 范式、 InternVL 范式、  DINO ）的   Attention Map ， 计 算它 们 在空 间 上的交并比（ IoU ）。  👉   复制以下   Prompt   给 你的   AI   编 程助手：  Plaintext  Act as an expert PyTorch and Vision-Language Model (VLM) researcher.  I am building a testing pipeline for adversarial attacks.  Please write a Python script that accomplishes the following:  1. Data Loading: Set up a basic pipeline to load image-text pairs from a local directory (simulate a subset of AdvBench), resizing all images to 336x336.  2. Model Initialization: Initialize three models using Hugging Face `transformers`:  - A CLIP-based vision encoder (e.g., from LLaVA-1.5)  - An InternVL-based vision encoder  - A ViT-DINOv2 model.  3. Attention Extraction: Write a function `extract_attention_maps(image, text_prompt)` that passes the image through these three models and extracts the self-attention maps from the LAST Transformer layer of each vision encoder.

4. IoU Calculation: Write a function to binarize these attention maps (keeping the top 10% highest attention pixels as 1, others as 0) and calculate the Intersection over Union (IoU) among the three masks.  5. Provide clear comments and modularize the code using object-oriented programming (OOP).  阶 段二： SAGA-MPC   核心算法开 发   (The Core Algorithm)  目 标 ：   将多模型梯度的聚合 逻辑 （ MPC ）与 阶 段性注意力掩 码 （ SAGA ） 结 合，并解决不同范式 间 的 梯度冲突（ Gradient Conflict ）。  👉   复制以下   Prompt   给 你的   AI   编 程助手：  Plaintext  Act as an expert in Adversarial Robustness and PyTorch optimization.  I am implementing a novel adversarial attack called 'SAGA-MPC'. I need you to write the core attack loop.  Algorithm Requirements:  1. Attack Parameters: image, target_text, epsilon (8/255), T (total stages = 10), K (iterations per stage = 5).  2. Stage-wise Dynamic Masking: At the beginning of each Stage 't', calculate the combined attention map from three surrogate VLMs. Generate a binary mask 'M' containing the Top-K% highest attention pixels.  3. Multi-Paradigm Optimization with PCGrad: Inside the stage loop (K iterations), calculate the adversarial loss for the three surrogates.  - Instead of summing the gradients directly, implement the PCGrad (Projected Conflicting Gradients) technique.  - If the dot product of two gradient vectors is negative, project one onto the normal of the other.  4. Masked Update: Apply the aggregated gradient ONLY to the pixels within the binary mask 'M'.  5. PGD Projection: Use standard PGD steps (sign of gradient) and clip the perturbation strictly within the $L_\infty$ epsilon bound and [0, 1] image range.  6. Return the final adversarial image. Optimize the tensor operations for GPU memory efficiency.

阶 段三： 规 模化 评测 与   Pareto   前沿可 视 化   (Benchmarking & Data Analysis)  目 标 ：   编 写批量攻 击 脚本，收集   ASR （攻 击 成功率）和   SSIM （ 结 构相似性）数据，并使用数据分析工 具 进 行清晰的可 视 化展示。  👉   复制以下   Prompt   给 你的   AI   编 程助手：  Plaintext  Act as an expert data scientist and Python developer.  I have completed a batch of adversarial attack experiments across different methods (Vanilla PGD, SAGA, MPC, SAGA-MPC) and recorded the results in a CSV file.  Please write a Python script using `pandas` and `matplotlib` to analyze and visualize this data:  1. CSV Structure assumption: The CSV has columns: `image_id`, `method`, `target_model`, `attack_success_rate` (0 or 1), and `ssim_score`.  2. Data Aggregation: Use `pandas` to group the data by `method` and `target_model`. Calculate the mean Attack Success Rate (ASR) and the mean SSIM for each method.  3. Pareto Frontier Visualization: Use `matplotlib` to create a high-quality, publication-ready scatter plot.  - X-axis: Mean SSIM (Higher is more stealthy).  - Y-axis: Mean ASR (Higher is more effective).  - Plot each attack `method` as a distinct point with different markers/colors.  - Draw a dashed line to highlight the Pareto Frontier.  - Ensure the plot has a grid, clear axis labels, a legend, and a professional style suitable for a CVPR/NeurIPS paper.  阶 段四：防御穿透极限 测试   (Defense Breaking)  目 标 ：   在 图 像 输 入目 标 模型之前，加入主流的防御 预处 理 层 ， 测试 攻 击 的 鲁 棒性。  👉   复制以下   Prompt   给 你的   AI   编 程助手：

Plaintext  Act as an AI Security researcher. I need to test the robustness of my generated adversarial images against standard defense mechanisms.  Please write a PyTorch evaluation module using `torchvision.transforms` that applies the following defense layers to an input batch of images before passing them to the target VLM:  1. Spatial Defense (Anti-SAGA): Implement Random Resized Cropping (RRC). Crop a random region (between 90% and 100% of the original area) and resize it back to the original tensor shape.  2. Denoising Defense (Anti-MPC): Apply a Gaussian Blur filter with a randomly selected kernel size (e.g., 3x3 or 5x5) and dynamic sigma.  3. JPEG Compression Simulation: Implement a differentiable or pseudo-differentiable JPEG compression transformation to simulate realistic image saving artifacts.  4. Wrap these defenses in a `DefensePipeline` class where I can easily toggle each defense on or off via boolean flags during the evaluation loop.